<h3><%= @book.title %></h3>

<p><%= simple_format(@book.body) %></p>

<%= link_to "Back", books_path(@books), class: 'btn btn-primary' %>


<% if can? :edit, Book %>
  <%= link_to "Edit Book", edit_book_path(@book), class: 'btn btn-primary btn-mini' %>
<% end %>
