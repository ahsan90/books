<h1> Learning Materials </h1>
<p> Click on your exam and then find materials for that exam </p>

<p> All materials are done by those who we have verified to achieve scores within the top 5% of their area - so you're in good company </p>



<ul class="nav nav-tabs">
  <li class="active">
    <a href="#all" data-toggle="tab">All</a>
  </li>
  <% Category.all.each do |category| %>
  <li>
    <a href="#<%= dom_id(category) %>" data-toggle="tab"><%= category.title %></a>
  </li>
  <% end %>
</ul>

<div class="tab-content">
  <div class="tab-pane active" id="all">
    <%= render Book.all %>
  </div>
  <% Category.all.each do |category| %>
  <div class="tab-pane" id="<%= dom_id(category)%>" >
    <%= render category.books %>
  </div>
  <% end %>
</div>

<% if can? :create, Book %>
  <%= link_to "Create New Book", new_book_path(Book.new), class: 'btn btn-large btn-block' %>
<% end %>
<% if can? :create, Category %>
  <%= link_to "Create New Category", new_category_path(Category.new), class: 'btn btn-large btn-block' %>
<% end %>


